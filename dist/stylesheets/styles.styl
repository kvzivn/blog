@import 'base';

.Rapper {
    margin: 5rem auto 0;
    width: 50rem;
}

.Icon-container {
    display: inline-block;
    float: right;
    margin-top: 1.25rem;
}

.Icon {
    width: 2.25rem;
    height: 2.25rem;
    opacity: .75;
    margin-left: 1rem;
}

.About {
    margin-bottom: 2rem;
    text-align: center;
}

.Archive {
    margin-bottom: 2rem;
}

    .Archive-post {
        padding: 2rem 1rem;
        border-bottom: 1px solid #ccc;
    }

    .Archive-postTitle {
        color: #323232;
        font-size: 1.5rem;
        font-weight: 400;
        -webkit-font-smoothing: antialiased;
    }

    .Archive-postDate {
        display: block;
    }